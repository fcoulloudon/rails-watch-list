<h1>New movie to add to list @list.name </h1>

<%= simple_form_for ([@list, @bookmark]) do |f| %>
  <%= f.label :comment %>
  <%= f.text_field :comment %><br>
  <%= f.association :movie, as: :select %><br>
  <%= f.submit %>
<% end %>